name: osqa

framework:
  type: python
  runtime: python27

mem: 128M

hooks:
  post-staging:
    - python manage.py syncdb --all
    - python manage.py migrate forum --fake

requirements:
  pypm:
    - django<=1.3.999
    - south
    - markdown
    - html5lib
    - python-openid
    - mysql-python

services:
  mysql-osqa : mysql

ignores: [".git","tmp"]

